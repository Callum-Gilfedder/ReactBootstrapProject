export default function NotFound404() {
    return (
        <>
        <h1>404 not found</h1>
        <p>This sections functionality has not been included because it either required a backend (i.e. search functionality - would require connection with a API with book data of some sort, this is beyond the scope of this front-end project) or I decided that developing it wouldn't further practice my React skills (i.e. a checkout page that would only be more of the same css stylings).</p>
        </>
    )
}